## Packages
(none needed, all required packages are in the base stack)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

The UI uses a dark mode "professional" aesthetic with glassmorphism elements and deep indigo/violet accents.
